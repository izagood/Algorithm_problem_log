#   ###   정답
```java
import java.util.*;
import java.util.stream.*;

class Solution {
    public int[] solution(String[] id_list, String[] report, int k) {
        /*
            1번에 1명
            신고 횟수 제한 x
            한 유저 여러번 가능 but 1회로 처리
            k번 이상 신고된 유저 - 정지
            신고한 모든 유저에게 메일 발송
            
            각 유저별로 처리 결과 메일을 받은 횟수를 배열에 담아 return
        */
        
        String[] ar1 = id_list;
        String[] ar2 = report;
        
        int[] answer = new int[ar1.length]; 
        
        /*
        key:value
        신고 당한거 카운트
        k랑 비교
        
        */
        
        Set<String> sset = new HashSet<>();
        for(String sr : ar2){
            sset.add(sr);
        }
        
        ar2 = sset.stream().toArray();
        
        //신고
        Map<String, Integer> simap = new HashMap<>();
        for(String sr : ar2){
            String[] ar3 = sr.split(" ");
            simap.put(ar3[1], simap.getOrDefault(ar3[1], 0) + 1);
        }
        
        List<String> slist = new ArrayList<>(); //신고된 사람
        for(String str : simap.keySet()){
            if(sim.get(str) >= k){
                slist.add(str);
            }
        }
        
        //메일
        int idx = 0;
        for(String str : ar1){
            for(String str2 : slist){
                if(){
                    answer[idx]++;
                }
            }
            idx++;
            
        }
        
        
        return answer;
    }
}
```

###   분석


###   참고할 만한 정답
```java
    소스코드
```