#   정답
```java

class Solution {
    public String longestCommonPrefix(String[] strs) {
        /*
            공통 prefix를 리턴 없으면 ""
        */
        String prefix = "";
        
        //각 단어를 하나씩 비교하면서 가야함.
        for(;;){
            for(int i=0; i<strs.length(); i++){
                strs[i].charAt(i);
                if(i+1 < strs.length()){

                }
            }
        }
        
        
        return prefix;
    }
}

```


#   분석